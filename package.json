{
  "name": "nextjs-bundle-actions-e2e",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "turbo run build",
    "build:webpack": "turbo run build:webpack",
    "lint": "turbo run lint",
    "test:snapshot": "node scripts/assert-snapshot.mjs"
  },
  "devDependencies": {
    "turbo": "2.10.12"
  }
}
